import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Customers',
    description: 'Customers мои Customers'
};

export default function CustomersPage() {
    return <p>Customers Page</p>
}